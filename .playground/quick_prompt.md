Create a macOS application using Tauri and React with the following features:

- Users can add folders as projects; when a folder is added, it opens in a VS Code instance embedded within the app.
- Users can manage multiple projects. All projects should appear in a sidebar on the left, while the selected project's VS Code instance is shown in the right panel.
